<?xml version="1.0" encoding="UTF-8"?>
<!-- Configures the Camel Context-->

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://camel.apache.org/schema/spring http://camel.apache.org/schema/spring/camel-spring.xsd">

	<!--Helper Bean to extract data from MQTT Topic and and to Message in csv values  -->
  	<bean id="myHelper" class="com.redhat.demo.iotdemo.routingService.MyHelper" />
  	
  	<bean id="rules" class="com.redhat.demo.iotdemo.routingService.BusinessRulesBean" init-method="init" />
  	
    
  	<!--Connection to activeMQ Broker running on same system  -->
  	<bean id="activemqGateway" class="org.apache.activemq.camel.component.ActiveMQComponent" >
    	<property name="connectionFactory">
      		<bean class="org.apache.activemq.ActiveMQConnectionFactory">
        		<property name="brokerURL" value="tcp://${BROKER_AMQ_TCP_SERVICE_HOST}:${BROKER_AMQ_TCP_SERVICE_PORT}" />
        		<property name="userName" value="${BROKER_AMQ_USERNAME}"/>
        		<property name="password" value="${BROKER_AMQ_PASSWORD}"/>
      		</bean>
    	</property>
  	</bean>
  	 
  	<!--Initiate access to properties -->
  	<bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer"/>

  <camelContext xmlns="http://camel.apache.org/schema/spring">
  <dataFormats>
    <bindy type="Csv" classType="com.redhat.demo.iotdemo.routingService.DataSet" id="bindyDataFormat"/>
  </dataFormats>
  <route id="messagesFromSensor">
  <from uri="mqtt:mqtt.temp.receiver?host=tcp://{{env:BROKER_AMQ_MQTT_SERVICE_HOST}}:{{env:BROKER_AMQ_MQTT_SERVICE_PORT}}&amp;subscribeTopicNames=iotdemo/#/#&amp;userName={{env:BROKER_AMQ_USERNAME}}&amp;password={{env:BROKER_AMQ_PASSWORD}}"/>
    <bean ref="myHelper" method="enhanceMessage" beanType="com.redhat.demo.routingService.MyHelper"/>
    <unmarshal ref="bindyDataFormat"/>
    <convertBodyTo type="java.lang.String"/>
    <to uri="activemqGateway:queue:message.to.rules"/>
  </route>
  

  <route id="processRules">
  <from uri="activemqGateway:queue:message.to.rules"/>
  <bean ref="myHelper" method="transform"/>
  <bean ref="rules" method="processRules"/>
  <choice>
  	<when>
  		<simple>${body.errorCode} == 0</simple>
  	    <to uri="activemqGateway:queue:message.to.datacenter"/>
  	</when>
  </choice>
  </route>
  
</camelContext>

</beans>